npx hardhat run scripts/deploy.ts --network 0g-testnet

change .env var

cd fl-engine
flwr run .

export PATH=~/go/bin:$PATH

cd ..
pnpm ask-run "How did the model improve on the last rounds?"

 




